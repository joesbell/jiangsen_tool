/**
 *
 *
 * @author jiangsen
 * @date 16/04/2022
 * @param data
 * @return {*}
 */
function tsRt(data) {
  console.log('235');
  return Math.sqrt(Math.pow(data.x, 2) + Math.pow(data.y, 2));
}
tsRt({ x: 12, y: 45 });
